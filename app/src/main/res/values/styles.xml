<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="DiscordButton" parent="Widget.MaterialComponents.Button">
        <item name="android:backgroundTint">#7289DA</item>
        <item name="android:textColor">#FFFFFF</item>
        <item name="iconStartPadding">8dp</item>
        <item name="iconEndPadding">8dp</item>
        <item name="iconSize">24dp</item>
    </style>

    <style name="TextInputLayoutCustomStyle" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">


        <item name="android:textColor">@android:color/white</item>    <!-- Color cuando el texto del hint está desactivado -->
        <item name="android:textColorHint">@android:color/white</item> <!-- Color cuando el texto del hint está activado -->
        <item name="hintTextColor">@android:color/white</item> <!-- Color del texto de hint -->

    </style>

</resources>